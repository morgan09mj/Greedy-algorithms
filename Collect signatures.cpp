#include<bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<pair<int,int>> intervals(n);
    for(int i=0;i<n;i++){
        int a, b;
        cin >> a >> b;
        intervals[i] = {a,b};
    }
    sort(intervals.begin(),intervals.end()); // ???? ???? ???? ?? ?? ???? ???? ????

    int ans = 1; // ????? ????? ????
    int lastPoint = intervals[0].second; // ???? ????? ?????? ?? ???? ???? ???

    for(int i=1;i<n;i++){
        if(intervals[i].first > lastPoint){ // ??? ???? ???? ??????? ???? ????? ?? ???? ???? ????? ??????
            ans++;
            lastPoint = intervals[i].second;
        }
        else if(intervals[i].second < lastPoint){ // ??? ???? ???? ?????? ?? ???? ???? ????? ?? ?? ??????? ???????
            lastPoint = intervals[i].second;
        }
    }
    cout << ans << endl;

    return 0;
}

